import React from 'react';
import useTitle from '../../Hook/useaTitle';

const Review = () => {
    useTitle('Review')
    return (
        <div>
            <h1>this is review</h1>
        </div>
    );
};

export default Review;